/*
import {
  Timeline, TimelineItem, TimelineSeparator, TimelineDot,
  TimelineConnector, TimelineContent, Typography
}
from "@mui/lab";
 */

/*
const OrderStatusTimeline: React.FC<{ timeline: { status: string; date?: string }[] }> = ({ timeline }) => (
  <Timeline>
    {timeline.map((step, index) => (
      <TimelineItem key={index}>
        <TimelineSeparator>
          <TimelineDot color={step.status === "Delivered" ? "success" : "primary"} />
          {index < timeline.length - 1 && <TimelineConnector />}
        </TimelineSeparator>
        <TimelineContent>
          <Typography>{step.status}</Typography>
          {step.date && <Typography color="text.secondary">{step.date}</Typography>}
        </TimelineContent>
      </TimelineItem>
    ))}
  </Timeline>
);
 */
